spring:
  main:
    banner-mode: off
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
logging:
  level:
    org.testcontainers: INFO
    org.springframework.orm.jpa.JpaTransactionManager: DEBUG
telegram-bot:
  name: mockname
  token: mocktoken